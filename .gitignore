cache/*/*
!cache/*/summary.json
# Keep per-channel cache summaries (cache/<channel>/<pkg>/summary.json)
cache/*/*/*
!cache/*/*/summary.json
repodata.json
*/filelist.txt
.venv/
*.tar.bz2
*.conda
table.csv
table_*.csv
__pycache__/
# pip-wheel analysis cache (transient; regenerated by the pip script)
cache_pip/
# download-index analysis cache (transient; regenerated by the download script)
cache_download/
# download-index markdown and CSV tables
table_download.csv
# transient temp root for disk-backed temp directory
wheel_tmp/
